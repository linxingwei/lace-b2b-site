import { ArrowRight } from "lucide-react";
import { articles } from "@/lib/site-data";
import { createPageMetadata } from "@/lib/seo";
export const metadata = createPageMetadata("Lace Sourcing Blog for Fashion Buyers", "Practical lace sourcing guides, material comparisons and trend insights for fashion brands, bridal designers and garment manufacturers.", "/blog");
export default function BlogPage() { return <main><section className="page-hero"><p className="eyebrow"><span /> Velorace insights</p><h1>Lace sourcing knowledge<br /><em>for global fashion buyers.</em></h1><p>Practical guides on materials, trends, supplier evaluation and product development from our Guangzhou sourcing team.</p></section><section className="section"><div className="blog-grid blog-grid-four">{articles.map((article) => <article className="blog-card" key={article.slug}><a href={`/blog/${article.slug}`}><img src={article.image} alt={article.alt} width="800" height="590" loading="lazy" decoding="async" /></a><span>{article.keyword}</span><h2><a href={`/blog/${article.slug}`}>{article.title}</a></h2><p>{article.description}</p><a className="text-link" href={`/blog/${article.slug}`}>Read article <ArrowRight size={14} /></a></article>)}</div></section></main>; }
